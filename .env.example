# Spotify server-side credentials. Copy this file to `.env.local` and replace
# the placeholders. Never prefix these with NEXT_PUBLIC_ or commit real values.
SPOTIFY_CLIENT_ID=your_client_id
SPOTIFY_CLIENT_SECRET=your_client_secret
SPOTIFY_REFRESH_TOKEN=your_refresh_token
