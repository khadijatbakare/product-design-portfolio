import { resumeContent } from '../content/bio'
import { siteConfig } from '../content/site'
export function ResumePage() { const resume = resumeContent; return <main className="resume-page section-wrap"><div className="resume-toolbar"><span>{resume.label}</span><button onClick={() => window.print()}>{resume.downloadLabel} ↓</button></div><header className="resume-header"><p className="resume-role">{siteConfig.role}</p><h1>{resume.headline}</h1><p>{resume.summary}</p><div className="resume-contact"><a href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a>{siteConfig.socials.map(item => <a href={item.href} key={item.label}>{item.label}</a>)}</div></header><div className="resume-layout"><section className="resume-main"><h2>Experience</h2>{resume.experience.map(item => <article className="resume-job" key={`${item.company}-${item.role}`}><div className="resume-job-heading"><div><h3>{item.role}</h3><p>{item.company} · {item.location}</p></div><span>{item.period}</span></div><p>{item.summary}</p><ul>{item.highlights.map(highlight => <li key={highlight}>{highlight}</li>)}</ul></article>)}</section><aside className="resume-aside"><section><h2>Capabilities</h2><ul className="resume-tags">{resume.capabilities.map(item => <li key={item}>{item}</li>)}</ul></section><section><h2>Education</h2>{resume.education.map(item => <div key={item.school}><h3>{item.qualification}</h3><p>{item.school}</p><span>{item.period}</span></div>)}</section><section><h2>Tools</h2><p>{resume.tools.join(' · ')}</p></section></aside></div><p className="resume-editor-note">{resume.note}</p></main> }
