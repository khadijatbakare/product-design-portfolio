import { footerContent, siteConfig } from '../../content/site'
import { ArrowLink, Container, Eyebrow } from '../primitives'
export function SiteFooter() { return <footer className="footer"><Container><Eyebrow>{footerContent.eyebrow}</Eyebrow><h2>{footerContent.headline.map(line => <span className="headline-line" key={line}>{line}</span>)}<em>{footerContent.emphasis}</em></h2><ArrowLink href={`mailto:${siteConfig.email}`}>{siteConfig.email}</ArrowLink><div className="footer-bottom"><span>© 2026 {siteConfig.name}</span><div>{siteConfig.socials.map(item => <ArrowLink href={item.href} direction="diagonal" key={item.label}>{item.label.toUpperCase()}</ArrowLink>)}{siteConfig.resumeUrl && <ArrowLink href={siteConfig.resumeUrl} direction="diagonal">RÉSUMÉ</ArrowLink>}</div><button onClick={() => window.scrollTo({ top: 0, behavior: 'smooth' })}>BACK TO TOP ↑</button></div></Container></footer> }
