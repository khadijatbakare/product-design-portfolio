import type { CaseStudy } from '../../types/content'
export function CaseStudyHeader({ study, navigate }: { study: CaseStudy; navigate: (route: string) => void }) { const meta = { role: study.role, team: study.team, timeline: study.timeline, platform: study.platform }; return <section className="case-hero section-wrap"><button className="back-link" onClick={() => navigate('home')}>← Back to work</button><p className="project-type">{study.disciplines.join(' · ').replace(/-/g, ' ').toUpperCase()}</p><h1>{study.title}</h1><p className="case-deck">{study.outcome}</p><div className="case-meta">{Object.entries(meta).map(([label, item]) => <div key={label}><span>{label.toUpperCase()}</span><strong>{item}</strong></div>)}</div></section> }
